(comment) @fold

(function_definition
  (header) @fold
  (END) @fold) @fold

(iff
  (IF) @fold
  (END) @fold) @fold

(iff
  (ELSE) @fold
  (END) @fold) @fold

(repeatt
  (REPEAT) @fold
  (UNTIL) @fold) @fold
